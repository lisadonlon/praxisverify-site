---
type: pii-sweep
business: praxisverify
generated: 2026-05-24
tags: [pii-sweep, security, automated]
---

# PII / Secret Sweep — praxisverify-site — 2026-05-24

## Summary

| Category | Tool | Findings |
|----------|------|----------|
| Secrets / credentials (working tree) | gitleaks 8.21.2 | **0** |
| Secrets / credentials (history, last 53 commits) | gitleaks 8.21.2 | **0** |
| Email-pattern matches | grep (`.html .md .txt .json .yml .yaml .js .ts .py`) | **19** across 6 files |
| Unique email domains detected | — | **1** (`@praxisverify.com`) |
| High-priority / unexpected findings | — | **None** |

Overall status: **CLEAN** — no secrets or credentials detected. All email addresses found are intentional public contact addresses at `@praxisverify.com`.

---

## Gitleaks — Working Tree (Top 10 Findings)

No findings. Scan completed in 204 ms across the full working tree.

---

## Gitleaks — History (Top 10 Findings)

No findings. Scan covered all 53 commits in the repository (the repo has fewer than 200 commits; gitleaks scanned the full history).

---

## Email-Pattern Domains (Top 10)

| Domain | Occurrences | Assessment |
|--------|-------------|------------|
| `@praxisverify.com` | 26 | **Intentional** — public contact email used throughout site HTML and docs |

No third-party domains, personal email addresses, or unexpected domains detected.

---

## Top 10 Files by Email-Pattern Count (Paths Only)

| File | Email hits |
|------|-----------|
| `index.html` | 5 |
| `privacy.html` | 4 |
| `contact.html` | 4 |
| `llms.txt` | 3 |
| `experts.html` | 2 |
| `terms.html` | 1 |

All hits are the site's public-facing contact address embedded in `mailto:` links, footer text, and privacy policy disclosures — all expected and intentional.

---

## Assessment

- **No secrets, API keys, tokens, or credentials** found in working tree or commit history.
- **No personal email addresses** (visitor/user data, form submissions) found in any tracked file.
- **No third-party service credentials** (form handler keys, analytics write keys, etc.) found.
- All email addresses present are the single intentional public contact address at `@praxisverify.com`.
  - Note: `hello@praxisverify.com`-style addresses are classified as intentional per sweep policy.

---

## Suggested Next Actions

1. **No immediate action required** — repo is clean.
2. Confirm that any future contact-form backend (if added) stores inbound form data **outside** this repo and never commits it.
3. If a third-party form handler (e.g. Formspree, Netlify Forms) is integrated, ensure the API key is supplied via an environment variable or Cloudflare secret — never hard-coded in HTML.
4. Re-run this sweep after any substantive change to `index.html`, `contact.html`, or any new `.env`-adjacent files.
5. Consider adding a `.gitleaks.toml` config to baseline the known `@praxisverify.com` contact address as an allowed pattern, reducing noise in future sweep reports.

---

*Generated by automated weekly PII sweep. All emails domain-redacted per GDPR policy. Raw gitleaks JSON not retained.*
