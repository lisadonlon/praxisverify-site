---
type: pii-sweep
business: praxisverify
generated: 2026-08-16
tags: [pii-sweep, security, automated]
---

# PII / Secret Sweep — praxisverify-site — 2026-08-16

## Summary

| Check | Result |
|-------|--------|
| Gitleaks working-tree findings | 0 |
| Gitleaks history findings (50 commits scanned) | 0 |
| Email-pattern hits (all files) | 20 occurrences |
| Unique email domains found | 1 |
| Unique email addresses found | 1 (`info@` — intentional) |
| HIGH PRIORITY secret hits | **None** |

All email occurrences resolve to the single intentional public contact address at `@praxisverify.com`. No credentials, API keys, tokens, or third-party personal data detected.

---

## Gitleaks — Working Tree (top 10 findings)

**0 findings.** Scan completed cleanly with no rule matches.

---

## Gitleaks — Git History (last 200 commits; 50 scanned)

**0 findings.** History scan completed cleanly with no rule matches.

---

## Email-Pattern Domains (top 10)

| Domain | Occurrence count |
|--------|-----------------|
| `*@praxisverify.com` | 28 |

All occurrences are the intentional public-facing address (`info@praxisverify.com`). No third-party, personal, or unexpected domains detected.

---

## Top 10 Files by Email Occurrence

| File path | Email hits |
|-----------|-----------|
| `./index.html` | 5 |
| `./privacy.html` | 4 |
| `./contact.html` | 4 |
| `./llms.txt` | 3 |
| `./experts.html` | 2 |
| `./terms.html` | 1 |
| `./diagnostics/2026-06-07-pii-sweep.md` | 1 |

> Note: contact-form and schema addresses (`hello@`, `info@`, `contact@`) at `praxisverify.com` are intentional per site design. Occurrences in `diagnostics/` are prior sweep reports referencing the domain by name.

---

## Suggested Next Actions

1. **No immediate action required** — sweep is clean on all dimensions.
2. **Verify new pages on merge**: `experts.html` and `terms.html` are newer additions; confirm their `info@praxisverify.com` references are intentional (they appear to be) and that no form-handler or webhook endpoint was added.
3. **Monitor for new pages**: if a contact form with a backend endpoint or webhook is added in future, ensure the endpoint URL and any bearer token are kept in environment variables — never committed.
4. **Re-run on new contributors**: if collaborators are added to the repo, run an ad-hoc sweep after their first push to validate no inadvertent credential exposure.
5. **History depth**: only 50 commits exist; scan covered full history. No action needed.

---

*All email local-parts are omitted per GDPR redaction policy. Domain-only reporting. Raw gitleaks JSON not committed.*
