---
type: pii-sweep
business: praxisverify
generated: 2026-05-10
tags: [pii-sweep, security, automated]
---

# PII / Secret Sweep — 2026-05-10

## Summary

| Check | Tool | Findings |
|-------|------|----------|
| Working-tree secret scan | gitleaks 8.21.2 | **0** |
| History secret scan (last 200 commits, 48 scanned) | gitleaks 8.21.2 | **0** |
| Email-pattern matches (custom grep) | grep | **22 occurrences across 16 lines** |
| Unique email domains | — | **1** |

No secrets, credentials, API keys, or form-handler tokens were detected in the working tree or commit history.

All email-pattern matches resolve to a single domain (`@praxisverify.com`) and are intentional business contact addresses.

---

## Gitleaks Working-Tree Findings

**None.** Scan completed clean.

---

## Gitleaks History Findings (last 200 commits)

**None.** 48 commits scanned; scan completed clean.

> Note: fewer than 200 commits exist in this repository; all commits were scanned.

---

## Top 10 Email-Pattern Domains

| Domain | Occurrences |
|--------|-------------|
| `*@praxisverify.com` | 22 |

All occurrences are intentional — these are the company's public contact addresses (e.g. hello@, contact@, privacy@ variants). No third-party or personal email domains were found.

---

## Top 10 File Paths by Email-Pattern Count

| File | Email occurrences |
|------|------------------|
| `./index.html` | 5 |
| `./privacy.html` | 3 |
| `./llms.txt` | 3 |
| `./experts.html` | 2 |
| `./contact.html` | 2 |
| `./terms.html` | 1 |

*All paths are in the repo root. No hits in subdirectories.*

---

## Suggested Next Actions

1. **No immediate action required** — gitleaks found zero secrets in both working tree and full commit history.
2. **Routine**: Re-run this sweep weekly or on any PR that touches HTML/JS to catch accidental credential inclusion before merge.
3. **Watch `experts.html` and `terms.html`**: These files were not present in the original CLAUDE.md file map — confirm they were added intentionally and contain only public-facing content.
4. **Consider `llms.txt`**: Contains 3 email-pattern hits; verify the file discloses only intended contact addresses for AI crawlers.
5. **Cloudflare headers**: Ensure CSP and security headers (per `CLOUDFLARE-SETUP.md`) remain active — the site has no server-side code, so header hygiene is the primary runtime defence.
6. **BrandKit sync reminder**: If any brand asset carrying embedded metadata (EXIF, ICC profiles) is updated, strip metadata before committing — not a finding today but a standing hygiene note.

---

*All emails domain-redacted. No raw gitleaks JSON included. Sweep run against branch `master` at HEAD.*
