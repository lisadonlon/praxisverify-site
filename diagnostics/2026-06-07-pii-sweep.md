---
type: pii-sweep
business: praxisverify
generated: 2026-06-07
tags: [pii-sweep, security, automated]
---

# PII / Secret Sweep — 2026-06-07

## Summary

| Category | Tool | Findings |
|----------|------|----------|
| Working-tree secrets | gitleaks 8.21.2 | 0 |
| History secrets (last 200 commits, 49 scanned) | gitleaks 8.21.2 | 0 |
| Email-pattern matches | grep (custom regex) | 19 lines across 6 files |
| Unique email domains | — | 1 (`@praxisverify.com`) |

**Overall status: PASS — no secrets or unintended credentials detected.**

All email-pattern matches are intentional public contact addresses at `@praxisverify.com`
(contact-form links, `mailto:` hrefs, structured data), consistent with a public marketing site.

---

## Working-Tree Findings (gitleaks)

**0 findings.** gitleaks detected no secrets, tokens, or credentials in the working tree.

---

## History Findings (gitleaks, last 200 commits)

**0 findings.** 49 commits scanned. No secrets detected in git history.

---

## Email-Pattern Domains (top 10)

| Domain | Occurrences |
|--------|-------------|
| `*@praxisverify.com` | 26 |

All matches are intentional. `hello@praxisverify.com` and `contact@praxisverify.com`-style
addresses are expected on a public marketing site. No third-party, personal, or unexpected
domains found.

---

## Top Files by Email-Pattern Count (top 10)

| File | Matching Lines |
|------|---------------|
| `./index.html` | 5 |
| `./privacy.html` | 4 |
| `./contact.html` | 4 |
| `./llms.txt` | 3 |
| `./experts.html` | 2 |
| `./terms.html` | 1 |

---

## Notes

- Email local parts are **not recorded** in this report (GDPR compliance).
- Line numbers are **not paired** with domain information.
- Raw gitleaks JSON output is not committed to this repo.
- `contact.html`, `privacy.html`, `terms.html`, `experts.html` — pages not in the original
  `CLAUDE.md` file map but present in the repo; confirm these are intentional additions.

---

## Suggested Next Actions

1. **No immediate action required** — zero secret findings in both working tree and history.
2. **Confirm extra pages are intentional:** `experts.html` and `terms.html` were not listed in
   the `CLAUDE.md` file map. Verify they are in scope and update `CLAUDE.md` + `sitemap.xml`
   if so.
3. **Re-run before any PR** that adds third-party service integrations (e.g. form handlers,
   analytics keys, webhook URLs) — those are the most likely future secret-leak vectors.
4. **Consider a `.gitleaks.toml` allowlist** for intentional contact emails so future runs
   produce zero noise and anomalies stand out more clearly.

---

*Generated by automated weekly PII sweep trigger. All emails domain-redacted.*
