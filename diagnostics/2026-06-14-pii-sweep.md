---
type: pii-sweep
business: praxisverify
generated: 2026-06-14
tags: [pii-sweep, security, automated]
---

# PII / Secret Sweep — praxisverify-site — 2026-06-14

## Summary

| Check | Tool | Findings |
|---|---|---|
| Working-tree secret scan | gitleaks 8.21.2 | 0 |
| History secret scan (last 200 commits) | gitleaks 8.21.2 | 0 (49 commits in repo) |
| Email-pattern grep (working tree) | custom regex | 19 matching lines, 1 unique domain |

**Overall status: CLEAN — no secrets detected; all email references are intentional contact addresses.**

---

## Working-Tree Secret Scan

gitleaks detected **0 leaks**. Scan completed in ~178 ms.

No API keys, tokens, credentials, or other secret patterns found in any tracked file.

---

## History Secret Scan

gitleaks scanned **49 commits** (the full repository history; fewer than the 200-commit window requested). **0 leaks** detected.

---

## Email-Pattern Findings (Working Tree)

### Top 10 Email-Pattern Domains

| Domain | Occurrences |
|---|---|
| `*@praxisverify.com` | 26 |

No other email domains detected in scoped file types (`.html`, `.md`, `.txt`, `.js`, `.ts`, `.json`, `.yml`, `.yaml`, `.py`).

> **Note:** Contact-form and public-contact addresses at `praxisverify.com` are intentional on this marketing site.
> All 26 occurrences resolve to the same publicly-disclosed contact address — no unexpected addresses found.

### Top 10 Files by Email-Pattern Hit Count

| File Path | Hit Count |
|---|---|
| `./index.html` | 5 |
| `./privacy.html` | 4 |
| `./contact.html` | 4 |
| `./llms.txt` | 3 |
| `./experts.html` | 2 |
| `./terms.html` | 1 |

> `experts.html` and `terms.html` are present in the repository but not listed in the CLAUDE.md file map. These pages contain only the standard `*@praxisverify.com` contact address — no anomalies — but the file map should be updated to reflect their existence.

*All email local-parts redacted per GDPR policy. Line numbers are not paired with domain references.*

---

## Suggested Next Actions

1. **No immediate action required on secrets** — gitleaks returned zero findings in both working-tree and history scans.
2. **Verify intentional contact address** — confirm `*@praxisverify.com` (info alias) is the correct public contact for all six pages that reference it; remove or update on any page where it is stale.
3. **Update CLAUDE.md file map** — `experts.html` and `terms.html` appear in the repo but are absent from the documented file map. Add entries or confirm they are drafts that should be `.gitignore`d.
4. **Re-run squirrelscan** if content was recently updated on any of the above pages, per standard workflow.
5. **Next sweep** — schedule for 2026-06-21.

---

*Generated by automated weekly PII sweep. Raw gitleaks JSON not stored. No email local-parts recorded.*
